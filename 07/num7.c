/* Write a program with a function that prints a simple pattern (like ***** on a single line).

Function should not take arguments and not return anything
Just print a row of stars
*/

#include <stdio.h>

void printStars() {
    printf("*****\n");
}

int main() {
    printStars();
    return 0;
}

// Output ----- *****